# GPT Image 2 / 2.5 更新与验证

日期：2026-09-21。API 基址：`https://api.supertoken.cc`。

## 本次调整

- 保留 `gpt-image-2`，新增两个 2.5 型号的选型说明：Flare 用于速度优先的日常任务，Sunburst 用于精细编辑及成品质量优先的任务。
- CLI 默认模型从 `gpt-image-2-count` 改为 `gpt-image-2.5-flare`。显式 `--model` 仍优先；旧别名保持可传入，由当前账号权限决定能否调用。
- 同步更新中英文 README、Skill、API 参考、安装元数据和配置提示。Skill 的安装名称与升级命令保持不变。
- CI 安装与升级检查增加 `references/model-selection.md`，防止分发时遗漏选型说明。

模型定位依据 AnySearch 检索并读取的 OpenAI 模型页，来源见[模型选择说明](../../skills/supertoken-gpt-image-2/references/model-selection.md)。SuperToken 静态文档当时尚未列出 2.5，实际 ID 通过当前 Token 的 `GET /v1/models` 确认。

## 实际请求结果

使用用户提供的模型 Token；凭据仅在临时进程内存及子进程环境中使用，未写入仓库、报告或命令参数。所有成功请求的退出码均为 0，stderr 为空。

生成提示词相同：蓝色陶瓷茶壶放在米色亚麻布上，左侧窗光，保留釉面和布料细节。编辑使用 Flare 原图，要求仅将茶壶改为翡翠绿色。

| 操作 | 模型 | quality | 总耗时 | PNG 字节数 |
| --- | --- | --- | ---: | ---: |
| 同步生成，未传 `--model` | `gpt-image-2.5-flare` | low | 18.87 秒 | 1,720,827 |
| 同步生成 | `gpt-image-2.5-sunburst` | high | 47.38 秒 | 1,592,277 |
| 同步生成 | `gpt-image-2` | low | 51.52 秒 | 1,726,145 |
| 本地图片编辑，未传 `--model` | `gpt-image-2.5-flare` | low | 30.96 秒 | 1,675,221 |
| 本地图片编辑 | `gpt-image-2.5-sunburst` | high | 55.86 秒 | 1,593,144 |

五张图均为 1024 × 1024 PNG，已打开检查内容。生成图符合茶壶产品摄影要求，编辑结果成功改为绿色。输出保存在本地忽略目录 `outputs/image-2.5-test/`，不随代码发布。

以上是单次连通性与结果保存验证，包含 DNS、网络和下载耗时，且质量参数不同，不能用来计算模型速度差或证明画质排名。

## 测试环境

宿主机 Python 3.14，使用实际 Skill CLI。通过 `npx --yes skills@1.5.19` 在临时项目安装到 Codex 和 Claude Code，并确认 Claude Code 的符号链接指向同一份 Skill；安装后的脚本完成模型列表查询与两个 2.5 编辑请求。

首次 Flare 请求返回图片结果，但本机代理将域名解析为 `198.18.0.0/15` 的 Fake-IP 地址，下载被现有安全检查拒绝。Docker 网络同样继承该解析；本次未完成 Docker 内生图测试。成功请求使用临时 DNS 包装层：仅对 Fake-IP 解析结果查询公网 DNS，再交回原脚本完成公网地址校验、TLS 连接、下载和保存。该环境适配没有加入分发代码，也没有放宽对私网地址或重定向的限制。

本次没有运行 Codex/Claude 对话端到端测试，也没有用资源 Key 查询异步任务。多图、Mask、Base64、异步及 Adobe 别名未纳入此次真实请求测试；已有离线回归测试仍保留。不要将同步成功解释为上述所有模式都已在 2.5 渠道验证。

## 后续复测

在正常公网 DNS 环境，进入安装后的 Skill 目录。通过 `scripts/setup.py` 安全配置模型 Token，或在当前 Bash/zsh 隐藏输入：

```bash
printf "SuperToken Model API Token: " >&2
IFS= read -r -s SUPERTOKEN_API_KEY
printf "\n" >&2
export SUPERTOKEN_API_KEY
python3 scripts/supertoken_image.py models
python3 scripts/supertoken_image.py generate --prompt "A blue ceramic teapot on beige linen" --output ./flare.png
python3 scripts/supertoken_image.py generate --model gpt-image-2.5-sunburst --quality high --prompt "A blue ceramic teapot on beige linen" --output ./sunburst.png
python3 scripts/supertoken_image.py generate --model gpt-image-2 --prompt "A blue ceramic teapot on beige linen" --output ./image-2.png
python3 scripts/supertoken_image.py edit --image ./flare.png --prompt "Change only the teapot to emerald green" --output ./flare-edit.png
python3 scripts/supertoken_image.py edit --model gpt-image-2.5-sunburst --quality high --image ./flare.png --prompt "Change only the teapot to emerald green" --output ./sunburst-edit.png
```

## 离线检查

新增回归先验证旧默认模型导致失败，再切换为 Flare 并验证默认值和三个显式模型均原样进入请求、保存结果。保留旧 `-count` 多图限制测试，并将其改为显式指定旧模型，避免错误依赖默认值。

`python3 -m unittest discover -s tests -p 'test_*.py' -q`：284 项通过。Python 编译、Skill frontmatter 校验和 `git diff --check` 通过。
